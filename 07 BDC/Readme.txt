BDC使用TCODE:SHD0

个人原则：
一.记录BDC时，不要有多余的操作动作哪怕是鼠标点击或键盘按击。
二.多余5下的操作不建议使用BDC，原因是页面太多判断即表示业务逻辑在当前界面组合情况很多，BDC即使成功，后期也很多业务情景组合导致出错。
三.可以使用BAPI的情况下尽量使用BAPI操作，BDC会根据配置变化而变，当业务配置改变时，BDC无端端会中枪（报错）。
四.如果最高单次处理数据过万的情况下要小心，需分批处理数据，因为数据量大的时候，BDC仍会有失败的情况。